global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/pflex.rules.yml

scrape_configs:
  - job_name: powerflex
    metrics_path: /metrics
    static_configs:
      - targets: ["pflex_exporter:9445"]
