apiVersion: v1
kind: Secret
metadata:
  name: pflex-exporter-secrets
  labels:
    app.kubernetes.io/name: pflex-exporter
type: Opaque
stringData:
  # Referenced as ${FLEX1_PASSWORD} in the ConfigMap's config.yaml.
  # Replace before applying; consider an external secret manager in production.
  FLEX1_PASSWORD: change-me
