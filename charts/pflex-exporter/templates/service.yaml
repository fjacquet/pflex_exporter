apiVersion: v1
kind: Service
metadata:
  name: {{ include "pflex-exporter.fullname" . }}
  labels:
    {{- include "pflex-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pflex-exporter.selectorLabels" . | nindent 4 }}
